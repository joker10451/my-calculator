# Руководство по мониторингу Считай.RU

## Обзор

Этот документ описывает систему мониторинга для отслеживания работоспособности, производительности и ошибок на production.

## Автоматическая проверка после деплоя

После каждого деплоя запускайте скрипт проверки:

```bash
# Проверка production
node scripts/check-production.js

# Или с кастомным URL
PRODUCTION_URL=https://your-domain.com node scripts/check-production.js
```

Скрипт проверяет:
- ✅ Доступность основных страниц
- ✅ Работу калькуляторов
- ✅ Статьи блога
- ✅ Sitemap.xml
- ✅ Robots.txt
- ✅ Время загрузки страниц

## Google Analytics

### Настройка

1. **Создайте Google Analytics 4 property**
   - Зайдите в Google Analytics
   - Admin → Create Property
   - Выберите GA4

2. **Получите Measurement ID**
   - Admin → Data Streams
   - Скопируйте Measurement ID (G-XXXXXXXXXX)

3. **Добавьте в проект**
   
   Создайте `.env`:
   ```
   VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
   ```

4. **Интегрируйте в код**
   
   Уже интегрировано через `src/services/analyticsService.ts`

### Отслеживаемые события

#### Автоматические события:
- **page_view** - просмотр страницы
- **scroll** - скролл страницы (25%, 50%, 75%, 100%)
- **click** - клики по элементам

#### Кастомные события:
- **calculator_use** - использование калькулятора
  - calculator_type: тип калькулятора
  - calculator_result: результат расчета

- **blog_article_read** - чтение статьи блога
  - article_id: ID статьи
  - article_title: заголовок статьи
  - reading_time: время чтения (секунды)
  - scroll_depth: глубина скролла (%)

- **blog_search** - поиск по блогу
  - search_query: поисковый запрос
  - results_count: количество результатов

- **blog_share** - шеринг статьи
  - article_id: ID статьи
  - share_platform: платформа (vk, telegram, whatsapp)

- **calculator_click_from_blog** - клик на калькулятор из статьи
  - article_id: ID статьи
  - calculator_type: тип калькулятора

### Настройка дашборда

1. **Создайте кастомный дашборд**
   - Reports → Library → Create new report

2. **Добавьте виджеты:**

   **Общая статистика:**
   - Активные пользователи (реальное время)
   - Просмотры страниц (за день/неделю/месяц)
   - Средняя длительность сессии
   - Bounce rate

   **Калькуляторы:**
   - Топ-5 используемых калькуляторов
   - Конверсия: просмотр → использование
   - Среднее время на странице калькулятора

   **Блог:**
   - Топ-10 статей по просмотрам
   - Среднее время чтения
   - Completion rate (дочитывание до конца)
   - Топ поисковых запросов
   - Клики на калькуляторы из статей

   **Источники трафика:**
   - Органический поиск
   - Прямые заходы
   - Социальные сети
   - Рефералы

### Настройка алертов

1. **Резкое падение трафика**
   - Admin → Custom Alerts
   - Condition: Sessions decrease by 50% compared to previous day
   - Notification: Email

2. **Увеличение bounce rate**
   - Condition: Bounce rate > 70%
   - Notification: Email

3. **Ошибки JavaScript**
   - Настройте через Google Tag Manager
   - Отслеживайте window.onerror

## Yandex Metrika

### Настройка

1. **Создайте счетчик**
   - Зайдите в Yandex Metrika
   - Добавить счетчик
   - Скопируйте ID счетчика

2. **Добавьте в проект**
   
   В `.env`:
   ```
   VITE_YANDEX_METRIKA_ID=XXXXXXXX
   ```

3. **Интегрируйте в код**
   
   Уже интегрировано через `src/hooks/useYandexMetrika.ts`

### Отслеживаемые цели

Настройте цели в Yandex Metrika:

1. **Использование калькулятора**
   - Тип: JavaScript событие
   - Идентификатор: calculator_use

2. **Чтение статьи блога**
   - Тип: JavaScript событие
   - Идентификатор: blog_article_complete

3. **Поиск по блогу**
   - Тип: JavaScript событие
   - Идентификатор: blog_search

4. **Клик на партнерскую ссылку**
   - Тип: JavaScript событие
   - Идентификатор: affiliate_click

### Вебвизор

Включите Вебвизор для записи сессий пользователей:
- Настройки → Вебвизор → Включить
- Полезно для анализа UX проблем

### Карта кликов

Используйте карту кликов для анализа:
- Где пользователи кликают чаще всего
- Какие элементы игнорируются
- Проблемы с навигацией

## Sentry (мониторинг ошибок)

### Настройка

1. **Создайте проект в Sentry**
   - Зайдите в Sentry.io
   - Create Project
   - Выберите React

2. **Установите SDK**
   ```bash
   npm install @sentry/react @sentry/tracing
   ```

3. **Инициализируйте в проекте**
   
   `src/main.tsx`:
   ```typescript
   import * as Sentry from "@sentry/react";
   
   Sentry.init({
     dsn: import.meta.env.VITE_SENTRY_DSN,
     integrations: [
       new Sentry.BrowserTracing(),
       new Sentry.Replay(),
     ],
     tracesSampleRate: 0.1,
     replaysSessionSampleRate: 0.1,
     replaysOnErrorSampleRate: 1.0,
     environment: import.meta.env.MODE,
   });
   ```

4. **Добавьте Error Boundary**
   ```typescript
   import { ErrorBoundary } from "@sentry/react";
   
   <ErrorBoundary fallback={<ErrorFallback />}>
     <App />
   </ErrorBoundary>
   ```

### Отслеживаемые ошибки

- JavaScript errors
- Unhandled promise rejections
- Network errors
- React component errors

### Настройка алертов

1. **Новые ошибки**
   - Alerts → Create Alert Rule
   - Condition: New issue is created
   - Notification: Email, Slack

2. **Частые ошибки**
   - Condition: Issue is seen more than 100 times in 1 hour
   - Notification: Email, Slack

3. **Критические ошибки**
   - Condition: Issue level is fatal
   - Notification: Email, SMS, Slack

## Uptime мониторинг

### UptimeRobot (рекомендуется)

1. **Создайте аккаунт**
   - Зайдите на UptimeRobot.com
   - Зарегистрируйтесь (бесплатный план)

2. **Добавьте мониторы**
   
   **Главная страница:**
   - Monitor Type: HTTP(s)
   - URL: https://schitay-online.ru
   - Monitoring Interval: 5 minutes
   - Alert Contacts: Email

   **Sitemap:**
   - URL: https://schitay-online.ru/sitemap.xml
   - Monitoring Interval: 15 minutes

   **API endpoints** (если есть):
   - URL: https://schitay-online.ru/api/health
   - Monitoring Interval: 5 minutes

3. **Настройте алерты**
   - Email при downtime
   - SMS при downtime > 5 минут (платная опция)
   - Webhook для интеграции со Slack

### Альтернативы

- **Pingdom** - более продвинутый, платный
- **StatusCake** - бесплатный план с ограничениями
- **Better Uptime** - современный интерфейс

## Performance мониторинг

### Lighthouse CI

Автоматический аудит производительности при каждом деплое.

1. **Установите Lighthouse CI**
   ```bash
   npm install -g @lhci/cli
   ```

2. **Создайте конфигурацию**
   
   `lighthouserc.json`:
   ```json
   {
     "ci": {
       "collect": {
         "url": [
           "https://schitay-online.ru",
           "https://schitay-online.ru/blog",
           "https://schitay-online.ru/calculator/mortgage"
         ],
         "numberOfRuns": 3
       },
       "assert": {
         "assertions": {
           "categories:performance": ["error", {"minScore": 0.9}],
           "categories:accessibility": ["error", {"minScore": 0.9}],
           "categories:best-practices": ["error", {"minScore": 0.9}],
           "categories:seo": ["error", {"minScore": 0.9}]
         }
       },
       "upload": {
         "target": "temporary-public-storage"
       }
     }
   }
   ```

3. **Запускайте после деплоя**
   ```bash
   lhci autorun
   ```

### Web Vitals

Отслеживайте Core Web Vitals:

- **LCP (Largest Contentful Paint)**: < 2.5s
- **FID (First Input Delay)**: < 100ms
- **CLS (Cumulative Layout Shift)**: < 0.1

Интегрируйте в Google Analytics:
```typescript
import { getCLS, getFID, getLCP } from 'web-vitals';

getCLS(console.log);
getFID(console.log);
getLCP(console.log);
```

## Дашборд мониторинга

### Создайте единый дашборд

Используйте Grafana или создайте кастомный дашборд:

**Метрики для отображения:**

1. **Доступность**
   - Uptime (%)
   - Response time (ms)
   - Status (up/down)

2. **Трафик**
   - Активные пользователи
   - Просмотры страниц
   - Новые пользователи

3. **Производительность**
   - LCP, FID, CLS
   - Time to First Byte
   - Bundle size

4. **Ошибки**
   - Количество ошибок (за час/день)
   - Топ-5 ошибок
   - Error rate (%)

5. **Бизнес-метрики**
   - Использование калькуляторов
   - Чтение статей блога
   - Клики на партнерские ссылки

## Чеклист мониторинга

### Ежедневно
- [ ] Проверить uptime (должен быть 99.9%+)
- [ ] Проверить новые ошибки в Sentry
- [ ] Проверить трафик в Google Analytics

### Еженедельно
- [ ] Проанализировать топ статей блога
- [ ] Проверить топ калькуляторов
- [ ] Проанализировать источники трафика
- [ ] Проверить bounce rate

### Ежемесячно
- [ ] Запустить полный Lighthouse audit
- [ ] Проанализировать тренды трафика
- [ ] Проверить конверсию калькуляторов
- [ ] Оптимизировать медленные страницы
- [ ] Обновить контент популярных статей

## Алерты и уведомления

### Критические (немедленное реагирование)
- 🔴 Сайт недоступен (downtime)
- 🔴 Критические ошибки JavaScript
- 🔴 Падение трафика > 80%

### Важные (реагирование в течение часа)
- 🟠 Медленная загрузка (> 5s)
- 🟠 Увеличение error rate > 5%
- 🟠 Падение трафика > 50%

### Информационные (проверить в течение дня)
- 🟡 Новые типы ошибок
- 🟡 Изменение bounce rate > 20%
- 🟡 Необычные паттерны трафика

## Полезные команды

```bash
# Проверка production
node scripts/check-production.js

# Lighthouse audit
npm run audit:performance

# Проверка bundle size
npm run audit:bundle

# Проверка accessibility
npm run audit:accessibility

# Полный аудит
npm run audit:all
```

## Контакты для алертов

Настройте уведомления на:
- 📧 Email: admin@schitay-online.ru
- 💬 Slack: #monitoring канал
- 📱 SMS: для критических алертов
