# 🚀 Быстрый старт: Аналитика

## ✅ Что уже работает

### 1. Google Analytics 4
- **ID**: `G-K1W27063WG`
- **Статус**: ✅ Установлен и работает
- **Отслеживает**:
  - Просмотры страниц
  - Клики по калькуляторам
  - Использование калькуляторов
  - Клики по партнерским виджетам
  - Конверсии (заявки)

### 2. Яндекс.Метрика
- **ID**: `106217699`
- **Статус**: ✅ Установлена и работает
- **Функции**:
  - Вебвизор (записи сессий)
  - Карта кликов (heatmaps)
  - Отслеживание ссылок
  - Точный показатель отказов

### 3. Система отслеживания конверсий
- **Статус**: ✅ Работает
- **Хранение**: localStorage
- **Отслеживает**:
  - Клики по виджетам партнеров
  - Конверсии по каждому партнеру
  - Метрики: CTR, Conversion Rate, средний чек

### 4. Дашборд аналитики
- **URL**: `https://schitay-online.ru/#/admin/analytics`
- **Статус**: ✅ Доступен
- **Показывает**:
  - Общую статистику конверсий
  - Метрики по партнерам
  - Разбивку по продуктам
  - Разбивку по калькуляторам
  - Экспорт данных

## 📊 Как проверить, что работает

### Google Analytics 4
1. Откройте https://analytics.google.com/
2. Выберите свойство с ID `G-K1W27063WG`
3. Перейдите в "Отчеты" → "Реального времени"
4. Откройте сайт в другой вкладке
5. Должны появиться активные пользователи

### Яндекс.Метрика
1. Откройте https://metrika.yandex.ru/
2. Выберите счетчик `106217699`
3. Перейдите в "Посетители" → "Вебвизор"
4. Должны быть записи сессий

### Дашборд
1. Откройте https://schitay-online.ru/#/admin/analytics
2. Должна отобразиться статистика
3. Если данных нет - это нормально, они накопятся со временем

## 🎯 Как отслеживать конверсии

### В коде партнерского виджета

```typescript
import { trackPartnerClick, trackConversion } from '@/lib/analytics/conversionTracking';

// При клике на кнопку виджета
const handleWidgetClick = () => {
  trackPartnerClick('tinkoff', 'mortgage', 'banner');
  // Открыть форму партнера
  window.open('https://partner-url.com', '_blank');
};

// При успешной заявке (если есть callback)
const handleConversion = () => {
  trackConversion({
    partner: 'tinkoff',
    productType: 'mortgage',
    calculatorType: 'mortgage',
    value: 5000000, // Сумма кредита
  });
};
```

### Партнеры для отслеживания
- `tinkoff` - Тинькофф
- `vtb` - ВТБ
- `alfabank` - Альфа-Банк
- `sberbank` - Сбербанк
- `raiffeisen` - Райффайзен
- `other` - Другие

### Типы продуктов
- `mortgage` - Ипотека
- `credit` - Кредит
- `deposit` - Вклад
- `insurance` - Страхование
- `investment` - Инвестиции
- `other` - Другое

## 🧪 A/B тестирование (опционально)

### Создание теста

```typescript
import { registerABTest, getVariant } from '@/lib/analytics/abTesting';

// Регистрация теста (один раз при загрузке)
registerABTest({
  id: 'widget-color-test',
  name: 'Тест цвета кнопки',
  variants: [
    {
      id: 'blue',
      name: 'Синяя кнопка',
      weight: 50,
      config: { color: 'blue', text: 'Оформить' }
    },
    {
      id: 'green',
      name: 'Зеленая кнопка',
      weight: 50,
      config: { color: 'green', text: 'Получить' }
    }
  ],
  startDate: new Date(),
  active: true
});

// Использование в компоненте
const variant = getVariant('widget-color-test');
const buttonColor = variant?.config.color || 'blue';
const buttonText = variant?.config.text || 'Оформить';
```

### Отслеживание метрик теста

```typescript
import { trackImpression, trackClick, trackConversion } from '@/lib/analytics/abTesting';

// При показе виджета
trackImpression('widget-color-test', variant.id);

// При клике
trackClick('widget-color-test', variant.id);

// При конверсии
trackConversion('widget-color-test', variant.id);
```

## 📈 Метрики для мониторинга

### Ключевые показатели
1. **CTR виджетов**: Клики / Показы × 100%
   - Цель: > 5%
   - Хорошо: > 10%

2. **Conversion Rate**: Конверсии / Клики × 100%
   - Цель: > 1%
   - Хорошо: > 3%

3. **Средний чек**: Сумма конверсий / Количество
   - Зависит от продукта

4. **Bounce Rate**: Отказы / Визиты × 100%
   - Цель: < 50%
   - Хорошо: < 30%

### Где смотреть
- **Google Analytics**: Отчеты → События → Конверсии
- **Яндекс.Метрика**: Отчеты → Цели
- **Дашборд**: https://schitay-online.ru/#/admin/analytics

## 🔧 Настройка целей в Яндекс.Метрике

1. Перейдите в https://metrika.yandex.ru/
2. Выберите счетчик `106217699`
3. Настройки → Цели → Добавить цель
4. Создайте цели:

### Цель 1: Клик по партнерскому виджету
- Тип: JavaScript событие
- Идентификатор: `partner_click`
- Название: "Клик по виджету партнера"

### Цель 2: Конверсия
- Тип: JavaScript событие
- Идентификатор: `conversion`
- Название: "Конверсия (заявка)"

### Цель 3: Использование калькулятора
- Тип: JavaScript событие
- Идентификатор: `calculator_interaction`
- Название: "Использование калькулятора"

## 🐛 Отладка

### Проверка в консоли браузера

```javascript
// Открыть DevTools (F12) → Console

// Проверить Google Analytics
gtag('event', 'test_event', { test: 'value' });

// Проверить Яндекс.Метрику
ym(106217699, 'reachGoal', 'test_goal');

// Проверить отслеживание конверсий
localStorage.getItem('partner_clicks');
localStorage.getItem('conversions');
```

### Режим разработки
В режиме разработки все события логируются в консоль:
```
GA4: Event tracked calculator_interaction
Yandex Metrika: goal tracked partner_click
Partner click tracked: { partner: 'tinkoff', calculatorType: 'mortgage' }
```

## 📝 Частые вопросы

### Q: Почему в GA4 нет данных?
**A**: Данные появляются с задержкой 24-48 часов. Проверьте "Реальное время" для мгновенных данных.

### Q: Почему ERR_BLOCKED_BY_CLIENT?
**A**: Это нормально. AdBlock блокирует Яндекс.Метрику. Не влияет на работу сайта.

### Q: Как экспортировать данные из дашборда?
**A**: Откройте дашборд → Кнопка "Экспорт данных" → Скачается JSON файл.

### Q: Как часто обновляются данные в дашборде?
**A**: Автоматически каждые 30 секунд.

### Q: Где хранятся данные о конверсиях?
**A**: В localStorage браузера (последние 100 конверсий) + отправляются в GA4 и Яндекс.Метрику.

## 🚀 Следующие шаги

1. ✅ Подождать 24-48 часов для накопления данных в GA4
2. ✅ Создать цели в Яндекс.Метрике
3. ✅ Интегрировать отслеживание в партнерские виджеты
4. ✅ Настроить еженедельные отчеты в GA4
5. ✅ Запустить A/B тесты для оптимизации конверсий

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Проверьте Network tab (запросы к analytics.google.com и mc.yandex.ru)
3. Проверьте, что JavaScript не заблокирован
4. Очистите кэш браузера (Ctrl+Shift+Del)

---

**Готово!** Аналитика работает и собирает данные. 🎉
