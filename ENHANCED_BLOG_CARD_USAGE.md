# Enhanced Blog Card - Руководство по использованию

## ✅ Что реализовано

Создан новый компонент `EnhancedBlogCard` с современным дизайном и анимациями.

### Основные возможности

1. **Три варианта карточек**:
   - `default` - стандартная карточка (280px изображение)
   - `featured` - избранная карточка (360px изображение, градиентная рамка)
   - `hero` - героическая карточка (500px изображение, полная ширина)

2. **Визуальные эффекты**:
   - ✨ Крупные изображения (60% высоты карточки)
   - 🎨 Градиентный оверлей на изображениях
   - 🔍 Zoom эффект при наведении (scale 1.05)
   - 💫 Плавные анимации Framer Motion
   - 🎯 Жирные тени (0 10px 30px)

3. **Категории с иконками**:
   - 🏠 Ипотека и кредиты (Home, Blue)
   - 🧮 Налоги и зарплата (Calculator, Emerald)
   - ⚡ ЖКХ (Zap, Amber)
   - ❤️ Здоровье (Heart, Red)
   - 👥 Семейное право (Users, Purple)
   - 🚗 Авто (Car, Cyan)
   - 📈 Инвестиции (TrendingUp, Lime)
   - ⚖️ Юридические вопросы (Scale, Gray)

## 📁 Файлы

```
src/components/blog/enhanced/
├── EnhancedBlogCard.tsx    # Основной компонент
├── index.ts                # Экспорт
└── README.md               # Документация

src/pages/
└── EnhancedBlogCardDemo.tsx  # Демо-страница
```

## 🚀 Быстрый старт

### 1. Импорт компонента

```tsx
import { EnhancedBlogCard } from '@/components/blog/enhanced';
```

### 2. Базовое использование

```tsx
<EnhancedBlogCard post={blogPost} />
```

### 3. С настройками

```tsx
<EnhancedBlogCard 
  post={blogPost}
  variant="featured"
  showExcerpt={true}
  showAuthor={true}
  showReadingTime={true}
/>
```

## 📊 Примеры использования

### Сетка статей (BlogPage)

```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
  {posts.map(post => (
    <EnhancedBlogCard 
      key={post.id}
      post={post}
      variant="default"
    />
  ))}
</div>
```

### Избранные статьи

```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
  {featuredPosts.map(post => (
    <EnhancedBlogCard 
      key={post.id}
      post={post}
      variant="featured"
    />
  ))}
</div>
```

### Hero секция

```tsx
<div className="max-w-4xl mx-auto mb-12">
  <EnhancedBlogCard 
    post={heroPost}
    variant="hero"
  />
</div>
```

## 🎨 Демо-страница

Посмотреть все варианты в действии:

```
http://localhost:5173/#/demo/enhanced-blog-card
```

Или после деплоя:

```
https://schitay-online.ru/#/demo/enhanced-blog-card
```

## 🔄 Замена существующего BlogCard

Чтобы использовать новый компонент вместо старого:

```tsx
// Было:
import { BlogCard } from '@/components/blog/BlogCard';

// Стало:
import { EnhancedBlogCard } from '@/components/blog/enhanced';

// Использование:
<EnhancedBlogCard post={post} variant="default" />
```

## 📋 Props

| Prop | Type | Default | Описание |
|------|------|---------|----------|
| `post` | `BlogPost` | required | Объект статьи |
| `variant` | `'default' \| 'featured' \| 'hero'` | `'default'` | Вариант карточки |
| `showExcerpt` | `boolean` | `true` | Показать описание |
| `showAuthor` | `boolean` | `true` | Показать автора |
| `showReadingTime` | `boolean` | `true` | Показать время чтения |

## 🎯 Следующие шаги

После тестирования компонента можно:

1. Заменить `BlogCard` на `EnhancedBlogCard` в `BlogPage.tsx`
2. Использовать вариант `featured` для избранных статей
3. Использовать вариант `hero` для главной статьи на странице блога
4. Настроить анимации под свои предпочтения

## ✅ Выполненные требования

- ✅ Requirement 1.1: Крупные изображения (60% высоты)
- ✅ Requirement 1.2: Zoom эффект при наведении (105%)
- ✅ Requirement 1.3: Жирные тени (0 10px 30px)
- ✅ Requirement 1.4: Градиентный оверлей
- ✅ Requirement 1.5: Яркие бейджи категорий с иконками
- ✅ Requirement 1.6: Три варианта карточек (default, featured, hero)

## 🐛 Известные ограничения

- Требуется Framer Motion (уже установлен)
- Требуется Tailwind CSS 3+ (уже настроен)
- Изображения должны быть в формате, поддерживаемом OptimizedImage

## 📝 Заметки

- Компонент полностью совместим с существующим типом `BlogPost`
- Все анимации оптимизированы для производительности (GPU-accelerated)
- Поддерживает accessibility (ARIA labels, semantic HTML)
- Автоматический prefetch при наведении для быстрой навигации
