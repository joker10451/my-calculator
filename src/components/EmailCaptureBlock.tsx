import { Link } from "react-router-dom";
import { ArrowRight, Lightbulb, TrendingUp, Clock, Tag } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";

export function EmailCaptureBlock() {
  /* ... содержимое, см. предыдущий шаг ... */
}

/** Component for capturing leads interested in promos. */
export default EmailCaptureBlock;