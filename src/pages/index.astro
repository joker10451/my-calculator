---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import HeroTeaser from '../components/HeroTeaser.astro';
import { getCollection } from 'astro:content';

const articles = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3)
  .map((a) => ({
    slug: a.slug,
    title: a.data.title,
    description: a.data.description,
    tags: a.data.tags,
    readingTime: Math.max(1, Math.round(a.body.split(/\s+/).length / 200)),
  }));

const samozanyatostTools = [
  { name: 'Калькулятор налога НПД', href: '/dlya-samozanyatyh/kalkulyator-naloga/' },
  { name: 'Сравнение НПД vs ИП', href: '/dlya-samozanyatyh/npd-vs-ip/' },
];

const samozanyatostBlogCount = articles.filter(a => a.tags && a.tags.includes('самозанятость')).length;

const faqItems = [
  {
    question: 'Сколько налогов платит самозанятый?',
    answer: 'Ставка НПД — 4% при работе с физлицами и 6% — с юрлицами. Лимит дохода — 2,4 млн ₽ в год. Страховые взносы платить не обязательно, но можно добровольно для пенсии.',
  },
  {
    question: 'Что выгоднее: самозанятость или ИП?',
    answer: 'Для дохода до 2,4 млн ₽ в год самозанятость выгоднее из-за меньшей ставки (4-6% против 6-15% УСН) и отсутствия обязательных страховых взносов (49 500 ₽ в год для ИП).',
  },
  {
    question: 'Может ли самозанятый получать пособия?',
    answer: 'Да. Самозанятые могут получать пособие по безработице (в минимальном размере) и единое пособие на детей при низком доходе. Больничные и декретные — только при добровольном страховании в СФР.',
  },
  {
    question: 'Как подтвердить доход самозанятого для ипотеки?',
    answer: 'Справка из приложения «Мой налог» за 6-12 месяцев. Банки (Т-Банк, ВТБ, Альфа-Банк) принимают её как подтверждение дохода. Средний чек и регулярность поступлений влияют на решение.',
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqItems.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: { '@type': 'Answer', text: item.answer },
  })),
};
---

<Layout
  title="Считай.RU — бесплатные финансовые калькуляторы и помощник по налогам"
  description="Калькулятор налога самозанятого, ипотеки, ЖКХ, ОСАГО. Рассчитайте бесплатно за 2 минуты. Без регистрации."
  canonical="https://schitay-online.ru/"
>
  <Fragment slot="header">
    <Header />
  </Fragment>

  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

  <!-- HERO: асимметричная сцена -->
  <section class="relative overflow-hidden bg-gradient-to-br from-gray-50 to-white dark:from-slate-900 dark:to-slate-800">
    <!-- Ambient фон: миллиметровая сетка (light), цифровые строки (dark) -->
    <div class="absolute inset-0 pointer-events-none opacity-[0.03] dark:opacity-[0.015] light-grid"></div>
    <div class="absolute inset-0 pointer-events-none opacity-0 dark:opacity-[0.025] dark-grid"></div>

    <div class="container-main py-10 md:py-16 lg:py-20">
      <div class="lg:flex lg:gap-10 lg:items-center">
        <!-- LEFT: заголовок + CTA -->
        <div class="lg:w-[38%] mb-8 lg:mb-0">
          <h1 class="font-heading text-[clamp(2rem,5vw,4.5rem)] leading-[1.08] font-black text-slate-900 dark:text-slate-50">
            Разберись<br />
            <span class="text-accent-600 dark:text-accent-300">с деньгами</span><br />
            за 2 минуты
          </h1>
          <p class="mt-4 text-sm md:text-base text-gray-500 dark:text-slate-300 leading-relaxed max-w-sm">
            Бесплатные калькуляторы для самозанятых, семей, ЖКХ и автовладельцев. Расчёты объективны — платят партнёры, не вы.
          </p>
          <div class="mt-6 flex flex-col items-start gap-3">
            <a href="/dlya-samozanyatyh/kalkulyator-naloga/"
              class="inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-accent-600 text-white font-bold text-sm hover:bg-accent-700 transition-colors shadow-lg shadow-accent-600/20">
              Посчитать свой налог
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
            <a href="/dlya-samozanyatyh/npd-vs-ip/"
               class="text-sm text-gray-500 dark:text-slate-400 hover:text-accent-600 dark:hover:text-accent-300 transition-colors">
              или сравнить режимы →
            </a>
          </div>
        </div>

        <!-- RIGHT: живой тизер -->
        <div class="lg:w-[62%] min-h-[280px]">
          <HeroTeaser />
        </div>
      </div>
    </div>
  </section>

  <!-- АУДИТОРИИ: неравные блоки -->
  <section class="py-14 md:py-18">
    <div class="container-main">
      <p class="text-xs font-bold text-gray-500 dark:text-slate-400 uppercase tracking-widest mb-6 text-center lg:text-left">Выберите свою ситуацию</p>
      <div class="grid md:grid-cols-3 gap-4 auto-rows-fr">
        <!-- Самозанятый — featured (живой флагман с превью) -->
        <a href="/dlya-samozanyatyh/"
          class="group relative p-7 rounded-2xl border-2 border-accent-200 dark:border-accent-700/60 bg-white dark:bg-slate-800/80 hover:shadow-xl hover:shadow-accent-600/10 dark:hover:shadow-accent-400/5 hover:-translate-y-0.5 transition-all duration-300 md:col-span-2 row-span-2 flex flex-col justify-between min-h-[240px]">
          <div>
            <div class="flex items-start justify-between mb-3">
              <div class="w-11 h-11 rounded-xl bg-accent-100 dark:bg-accent-900/40 flex items-center justify-center group-hover:scale-110 transition-transform">
                <svg class="w-5.5 h-5.5 text-accent-600 dark:text-accent-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.193 23.193 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 20 0 00-2 2v10a2 2 0 002 2z"/></svg>
              </div>
              <span class="px-2.5 py-1 text-[10px] font-bold rounded-full bg-emerald-100 dark:bg-emerald-900/50 text-emerald-700 dark:text-emerald-400 shrink-0">{samozanyatostTools.length} инструмента</span>
            </div>
            <h2 class="font-heading text-xl font-black text-slate-900 dark:text-slate-50 mb-1">Самозанятый / фрилансер</h2>
            <p class="text-sm text-gray-500 dark:text-slate-300 max-w-md mb-3">Налоги, вычеты, пенсия. Посчитайте, сравните с ИП и узнайте, сколько можно сэкономить.</p>
            <div class="space-y-1.5 mb-2">
              {samozanyatostTools.map((t) => (
                <span class="flex items-center gap-2 text-xs text-accent-600 dark:text-accent-400 font-medium group-hover:translate-x-0.5 transition-transform">
                  <svg class="w-3 h-3 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                  {t.name}
                </span>
              ))}
            </div>
          </div>
          <div class="flex items-center justify-between mt-2">
            <span class="inline-flex items-center gap-1 text-sm font-bold text-accent-600 dark:text-accent-400 group-hover:gap-2 transition-all">
              Перейти →</span>
            <span class="text-[10px] text-gray-500 dark:text-slate-400">{samozanyatostBlogCount} {samozanyatostBlogCount === 1 ? 'статья' : 'статьи'} в блоге</span>
          </div>
        </a>

        <!-- Семья -->
        <a href="/dlya-semi/"
          class="group p-6 rounded-2xl border border-gray-200 dark:border-slate-700/50 bg-white dark:bg-slate-800/80 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex flex-col justify-between min-h-[100px]">
          <div>
            <div class="w-9 h-9 rounded-lg bg-warm-50 dark:bg-warm-900/40 flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
              <svg class="w-4.5 h-4.5 text-warm-600 dark:text-warm-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
            </div>
            <h2 class="font-heading font-bold text-base text-slate-900 dark:text-slate-50 mb-1">Семья</h2>
            <p class="text-xs text-gray-500 dark:text-slate-400">Ипотека, алименты, пособия на детей</p>
          </div>
          <span class="mt-3 inline-flex items-center text-xs font-semibold text-warm-600 dark:text-warm-400 group-hover:gap-1.5 transition-all gap-1">Скоро →</span>
        </a>

        <!-- ЖКХ -->
        <a href="/zhkh/"
          class="group p-6 rounded-2xl border border-gray-200 dark:border-slate-700/50 bg-white dark:bg-slate-800/80 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex flex-col justify-between min-h-[100px]">
          <div>
            <div class="w-9 h-9 rounded-lg bg-accent-50 dark:bg-accent-900/40 flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
              <svg class="w-4.5 h-4.5 text-accent-600 dark:text-accent-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/></svg>
            </div>
            <h2 class="font-heading font-bold text-base text-slate-900 dark:text-slate-50 mb-1">ЖКХ</h2>
            <p class="text-xs text-gray-500 dark:text-slate-400">Нормативы, переплата, поверка счётчиков</p>
          </div>
          <span class="mt-3 inline-flex items-center text-xs font-semibold text-accent-600 dark:text-accent-400 group-hover:gap-1.5 transition-all gap-1">Скоро →</span>
        </a>

        <!-- Авто (full-width bottom row — no empty cells) -->
        <a href="/avto/"
          class="group p-5 md:p-6 rounded-2xl border border-gray-200 dark:border-slate-700/50 bg-white dark:bg-slate-800/80 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 md:col-span-3 flex flex-col md:flex-row md:items-center md:justify-between min-h-[70px]">
          <div class="flex items-center gap-4">
            <div class="w-9 h-9 rounded-lg bg-warm-50 dark:bg-warm-900/40 flex items-center justify-center shrink-0 group-hover:scale-110 transition-transform">
              <svg class="w-4.5 h-4.5 text-warm-600 dark:text-warm-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
            </div>
            <div>
              <h2 class="font-heading font-bold text-base text-slate-900 dark:text-slate-50">Авто</h2>
              <p class="text-xs text-gray-500 dark:text-slate-400">ОСАГО, КАСКО, топливо, штрафы</p>
            </div>
          </div>
          <span class="mt-3 md:mt-0 inline-flex items-center text-xs font-semibold text-warm-600 dark:text-warm-400 group-hover:gap-1.5 transition-all gap-1">Скоро →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- SOCIAL PROOF -->
  <section class="py-14 md:py-18 bg-white dark:bg-slate-900">
    <div class="container-main">
      <div class="grid md:grid-cols-3 gap-6 max-w-4xl mx-auto text-center">
        <div class="p-5">
          <div class="text-4xl font-black text-accent-600 dark:text-accent-300 mb-1">100%</div>
          <p class="text-sm text-gray-500 dark:text-slate-400">Бесплатно навсегда. Без скрытых подписок.</p>
        </div>
        <div class="p-5">
          <div class="text-4xl font-black text-accent-600 dark:text-accent-300 mb-1">{samozanyatostTools.length}</div>
          <p class="text-sm text-gray-500 dark:text-slate-400">Калькулятора для самозанятых — считаем налог НПД и сравниваем с ИП</p>
        </div>
        <div class="p-5">
          <div class="text-4xl font-black text-accent-600 dark:text-accent-300 mb-1">0 ₽</div>
          <p class="text-sm text-gray-500 dark:text-slate-400">Бесплатно навсегда. Партнёрские ссылки — без навязывания.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- МОНЕТИЗАЦИЯ: асимметричный блок -->
  <section class="py-14 md:py-18 border-t border-gray-100 dark:border-slate-700/30">
    <div class="container-main">
      <div class="flex flex-col lg:flex-row gap-8 lg:gap-16">
        <div class="lg:w-2/5 shrink-0">
          <p class="text-xs font-bold text-gray-500 dark:text-slate-400 uppercase tracking-widest mb-4">Честность</p>
          <h2 class="font-heading text-3xl md:text-4xl font-black text-slate-900 dark:text-slate-50 leading-[1.1] mb-4">
            Бесплатно для вас.<br />
            <span class="text-accent-600 dark:text-accent-300">Навсегда.</span>
          </h2>
          <p class="text-sm text-gray-500 dark:text-slate-300 leading-relaxed">
            Расчёты объективны — мы не продаём места в сравнениях и не ставим баннеры. Зарабатываем на партнёрских программах: если продукт вам подошёл и вы оформили его по нашей ссылке, партнёр платит нам комиссию. На цифры калькулятора это не влияет — он считает одинаково в любом случае.
          </p>
          <a href="/partners/" class="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-accent-600 dark:text-accent-300 hover:gap-2 transition-all">
            Подробнее →</a>
        </div>
        <div class="lg:w-3/5 flex items-center justify-center lg:justify-end">
          <div class="grid grid-cols-2 gap-3 w-full max-w-md">
            <div class="p-4 rounded-xl bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-slate-700/50">
              <div class="text-xl font-black text-slate-900 dark:text-slate-50 mb-1">0 ₽</div>
              <p class="text-xs text-gray-500 dark:text-slate-400">с вас — навсегда. Платит банк или сервис, не вы.</p>
            </div>
            <div class="p-4 rounded-xl bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-slate-700/50">
              <div class="text-xl font-black text-slate-900 dark:text-slate-50 mb-1">Независимо</div>
              <p class="text-xs text-gray-500 dark:text-slate-400">ссылки не влияют на расчёты. Алгоритм един для всех.</p>
            </div>
            <div class="p-4 rounded-xl bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-slate-700/50">
              <div class="text-xl font-black text-slate-900 dark:text-slate-50 mb-1">Без рейтингов</div>
              <p class="text-xs text-gray-500 dark:text-slate-400">не берём денег за места в сравнениях продуктов.</p>
            </div>
            <div class="p-4 rounded-xl bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-slate-700/50">
              <div class="text-xl font-black text-slate-900 dark:text-slate-50 mb-1">По закону</div>
              <p class="text-xs text-gray-500 dark:text-slate-400">партнёрские материалы маркируем по 38-ФЗ (реклама, токен ОРД).</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- БЛОГ -->
  <section class="py-14 md:py-18 bg-gray-50 dark:bg-slate-900/50">
    <div class="container-main">
      <div class="flex items-center justify-between mb-8">
        <h2 class="font-heading text-xl md:text-2xl font-black text-slate-900 dark:text-slate-50">
          Последние статьи
        </h2>
        <a href="/blog/" class="text-sm font-semibold text-accent-600 dark:text-accent-300 hover:underline">Все статьи →</a>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-5">
        {articles.map((a) => (
          <a href={`/blog/${a.slug}/`} class="group p-6 rounded-2xl bg-white dark:bg-slate-800/80 border border-gray-200 dark:border-slate-700/50 hover:shadow-lg transition-shadow">
            {a.tags.length > 0 && (
              <span class="text-[10px] font-semibold text-accent-600 dark:text-accent-300 uppercase tracking-wider">{a.tags[0]}</span>
            )}
            <h3 class="font-heading font-bold text-sm mt-2 mb-2 text-slate-900 dark:text-slate-50 group-hover:text-accent-600 dark:group-hover:text-accent-300 transition-colors leading-snug">
              {a.title}
            </h3>
            <p class="text-xs text-gray-500 dark:text-slate-400 leading-relaxed">{a.description}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-14 md:py-18 bg-white dark:bg-slate-900">
    <div class="container-main max-w-3xl">
      <h2 class="font-heading text-xl md:text-2xl font-black text-center text-slate-900 dark:text-slate-50 mb-8">
        Часто задаваемые вопросы
      </h2>
      <div class="space-y-3" itemscope itemtype="https://schema.org/FAQPage">
        {faqItems.map((item) => (
          <details class="group p-5 rounded-2xl border border-gray-200 dark:border-slate-700/50 bg-white dark:bg-slate-800/80 open:shadow-md transition-all" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
            <summary class="flex items-center justify-between gap-3 cursor-pointer font-semibold text-slate-900 dark:text-slate-50" itemprop="name">
              <span>{item.question}</span>
              <svg class="w-5 h-5 text-gray-500 dark:text-slate-400 shrink-0 group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
            </summary>
            <div class="mt-3 text-sm text-gray-600 dark:text-slate-300 leading-relaxed" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
              <div itemprop="text">{item.answer}</div>
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Smooth FAQ open/close */
  details[open] > div {
    max-height: 500px;
    opacity: 1;
    padding-top: 0;
  }

  /* Light grid ambient */
  .light-grid {
    background-image: repeating-linear-gradient(0deg, transparent, transparent 39px, #000 39px, #000 40px),
      repeating-linear-gradient(90deg, transparent, transparent 39px, #000 39px, #000 40px);
  }

  /* Dark grid — sparse code-rain pattern (subtle) */
  .dark-grid {
    background-image:
      radial-gradient(circle at 10% 20%, rgba(59, 130, 246, 0.03) 0%, transparent 50%),
      radial-gradient(circle at 90% 60%, rgba(251, 146, 60, 0.025) 0%, transparent 50%),
      repeating-linear-gradient(0deg, transparent, transparent 59px, rgba(148, 163, 184, 0.04) 59px, rgba(148, 163, 184, 0.04) 60px),
      repeating-linear-gradient(90deg, transparent, transparent 59px, rgba(148, 163, 184, 0.04) 59px, rgba(148, 163, 184, 0.04) 60px);
  }

  /* Scroll-triggered animation class (optional progressive enhancement) */
  @media (prefers-reduced-motion: no-preference) {
    .reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
