# Статус интеграции реферальной системы

## ✅ Что реализовано

### 1. Конфигурация партнерских ссылок
**Файл:** `src/config/affiliateLinks.ts`

- Структура для хранения партнерских ссылок
- Функции `getAffiliateLink()` и `getCommissionInfo()` для получения ссылок
- Примеры ссылок для Сбербанка и ВТБ (с UTM-метками)
- Поддержка разных типов продуктов (ипотека, вклады)

**Что нужно сделать:**
- Заменить примеры на ваши реальные партнерские ссылки
- Добавить ссылки для всех банков-партнеров

### 2. Система трекинга
**Файл:** `src/lib/analytics/referralTracking.ts`

- Класс `ReferralTracker` для отслеживания кликов и конверсий
- Автоматическое сохранение в localStorage
- Интеграция с Yandex Metrika и Google Analytics
- Статистика по кликам (по банкам, продуктам, источникам)
- Расчет потенциального дохода
- Хук `useReferralTracking()` для React компонентов
- Автоматическая очистка старых событий (>90 дней)

**Готово к использованию!**

### 3. React компоненты
**Файл:** `src/components/ReferralButton.tsx`

Три готовых компонента:

1. **ReferralButton** - основная кнопка оформления
   - Автоматический трекинг кликов
   - Открытие ссылки в новой вкладке
   - Настраиваемый внешний вид
   - Опциональное отображение комиссии

2. **ReferralIconButton** - компактная кнопка с иконкой
   - Для использования в списках и карточках

3. **ReferralLink** - текстовая ссылка с трекингом
   - Для встраивания в текст

**Готово к использованию!**

### 4. Примеры использования
**Файл:** `src/components/examples/ReferralButtonExample.tsx`

Четыре готовых примера:
1. Базовое использование в результатах калькулятора
2. Список рекомендаций с кнопками
3. Текстовая ссылка в описании
4. Интеграция с результатами MatchingAlgorithm

**Используйте как шаблоны!**

### 5. Панель администратора
**Файл:** `src/components/admin/ReferralDashboard.tsx`

Полнофункциональная панель со статистикой:
- Общее количество кликов
- Потенциальный доход
- Графики по банкам (столбчатая диаграмма)
- Графики по типам продуктов (круговая диаграмма)
- Источники трафика
- История последних 20 кликов
- Кнопка очистки старых событий

**Готово к использованию!**

### 6. Документация
**Файл:** `docs/REFERRAL_SYSTEM_GUIDE.md`

Полное руководство на русском языке:
- Описание всех компонентов
- Примеры кода
- Инструкции по интеграции
- Описание типов данных
- Настройка аналитики

**Готово к использованию!**

## 📋 Следующие шаги

### Шаг 1: Добавить реальные партнерские ссылки
```typescript
// Откройте src/config/affiliateLinks.ts
// Замените примеры на ваши реальные ссылки

export const AFFILIATE_LINKS: Record<string, AffiliateLink> = {
  'sberbank-mortgage': {
    url: 'ВАША_РЕАЛЬНАЯ_ССЫЛКА_С_UTM',
    bankId: 'sberbank',
    productType: 'mortgage',
    commission: 3000, // ваша комиссия в рублях
    description: 'Ипотека Сбербанк'
  },
  // Добавьте все ваши ссылки
};
```

### Шаг 2: Интегрировать кнопки в калькуляторы

#### Пример для MortgageCalculator:

```tsx
// В файле src/components/calculators/MortgageCalculator.tsx
import { ReferralButton } from '@/components/ReferralButton';

// Добавьте в секцию результатов (Results Sidebar):
<div className="space-y-3 pt-6 border-t border-primary/10">
  {/* Кнопка оформления в рекомендуемом банке */}
  <ReferralButton
    product={recommendedProduct}
    source="calculator"
    variant="default"
    size="lg"
    className="w-full"
  >
    Оформить ипотеку
  </ReferralButton>

  {/* Существующие кнопки */}
  <Button className="w-full gap-2 py-6 text-lg" variant="hero" onClick={handleDownload}>
    <Download className="w-6 h-6" />
    Скачать отчет
  </Button>
  {/* ... */}
</div>
```

#### Аналогично для других калькуляторов:
- `DepositCalculator.tsx` - кнопка "Открыть вклад"
- `CreditCalculator.tsx` - кнопка "Оформить кредит"

### Шаг 3: Создать страницу администратора

```tsx
// Создайте файл src/pages/AdminReferralPage.tsx
import { ReferralDashboard } from '@/components/admin/ReferralDashboard';

export default function AdminReferralPage() {
  return (
    <div className="container mx-auto py-8">
      <ReferralDashboard />
    </div>
  );
}
```

Добавьте роут в ваш роутер (например, `/admin/referrals`).

### Шаг 4: Настроить Yandex Metrika (если еще не настроена)

Система автоматически отправляет события в Yandex Metrika, если она настроена.

Убедитесь, что в вашем HTML есть:
```html
<script>
  window.YM_COUNTER_ID = 'ВАШ_COUNTER_ID';
</script>
```

### Шаг 5 (опционально): Серверная интеграция

Если хотите сохранять события на сервере:

1. Создайте API endpoint `/api/analytics/referral`
2. Раскомментируйте код в `src/lib/analytics/referralTracking.ts`:

```typescript
private static sendToAnalytics(eventName: string, data: any): void {
  // ... существующий код ...
  
  // Раскомментируйте:
  fetch('/api/analytics/referral', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ event: eventName, data })
  });
}
```

## 🎯 Быстрый старт

### Минимальная интеграция (5 минут):

1. **Добавьте свои ссылки** в `src/config/affiliateLinks.ts`

2. **Добавьте кнопку в калькулятор:**
```tsx
import { ReferralButton } from '@/components/ReferralButton';

// В результатах калькулятора:
<ReferralButton
  product={{
    id: 'sber-mortgage',
    bank_id: 'sberbank',
    product_type: 'mortgage',
    name: 'Ипотека',
    interest_rate: 6.0,
    bank: {
      id: 'sberbank',
      name: 'Сбербанк',
      short_name: 'Сбербанк',
      is_partner: true,
      commission_rate: 0.5,
      website_url: 'https://www.sberbank.ru'
    }
  }}
  source="calculator"
/>
```

3. **Готово!** Клики автоматически отслеживаются.

## 📊 Просмотр статистики

Используйте компонент `ReferralDashboard` для просмотра:
- Количества кликов
- Потенциального дохода
- Графиков по банкам и продуктам
- Истории кликов

## 🔗 Связь с существующей системой

Реферальная система интегрирована с:

1. **Типами банков** (`src/types/bank.ts`):
   - `is_partner` - флаг партнерского банка
   - `commission_rate` - процент комиссии
   - `referral_terms` - условия реферальной программы

2. **MatchingAlgorithm** (`src/lib/comparison/MatchingAlgorithm.ts`):
   - `calculateReferralValue()` - расчет комиссии
   - Учет партнерского статуса при ранжировании

3. **Типами рекомендаций**:
   - `referral_link` - партнерская ссылка
   - `referral_id` - ID реферала
   - `commission_potential` - потенциальная комиссия

## ❓ Вопросы и ответы

**Q: Где хранятся данные о кликах?**
A: В localStorage браузера. Опционально можно настроить отправку на сервер.

**Q: Как добавить новую партнерскую ссылку?**
A: Откройте `src/config/affiliateLinks.ts` и добавьте новую запись в объект `AFFILIATE_LINKS`.

**Q: Как посмотреть статистику?**
A: Используйте компонент `ReferralDashboard` или вызовите `ReferralTracker.getClickStatistics()`.

**Q: Работает ли трекинг без интернета?**
A: Да, события сохраняются локально и будут отправлены в аналитику при следующем подключении.

**Q: Можно ли отслеживать конверсии?**
A: Да, используйте `ReferralTracker.trackConversion()` или хук `useReferralTracking().trackConversion()`.

## 📁 Структура файлов

```
src/
├── config/
│   └── affiliateLinks.ts          # Конфигурация ссылок
├── lib/
│   └── analytics/
│       └── referralTracking.ts    # Система трекинга
├── components/
│   ├── ReferralButton.tsx         # React компоненты
│   ├── admin/
│   │   └── ReferralDashboard.tsx  # Панель администратора
│   └── examples/
│       └── ReferralButtonExample.tsx  # Примеры использования
└── types/
    └── bank.ts                    # Типы (уже существует)

docs/
└── REFERRAL_SYSTEM_GUIDE.md       # Полная документация
```

## ✨ Готово к использованию!

Все компоненты реализованы и готовы к интеграции. Следуйте инструкциям выше для быстрого старта.
